local widget = require("widget")
local t = {}

local createScene = function (mainFunc, shapeArray, shapeArrayParameters, group)
	
end
	t.createScene = createScene

local delayedEnterScene = function (mainFunc, shapeArray, shapeArrayParameters, storyboard, myGameSettings)

	

end
	t.delayedEnterScene = delayedEnterScene

local didExitScene = function (mainFunc, shapeArray, shapeArrayParameters)

    

end
	t.didExitScene = didExitScene

return t
