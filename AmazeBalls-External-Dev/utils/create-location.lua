local createLocation = function(xScreen, yScreen, xSquare, ySquare)
    return {
        xScreen = xScreen,
        yScreen = yScreen,
        xSquare = xSquare,
        ySquare = ySquare
    };
end

return createLocation;